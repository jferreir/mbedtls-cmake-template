This code shows how to use cmake FetchContent to consume mbedtls library in a project 

# Building

```sh
git clone https://github.com/jferreir/mbedtls-cmake-template
mkdir build && cd build
cmake -GNinja ..
ninja
```
